# Как добавить переменную окружения в windows

```sh
set BUG_ID={нужное число}&& npm run test
```
# Никаких пробелов между значением переменной и && (что?)
команду выполнять только в cmd, в powershell ничего не работает